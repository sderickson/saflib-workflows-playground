{
  "name": "@your-org/deploy-dev",
  "description": "Docker compose setup for dev environment",
  "type": "module",
  "scripts": {
    "dev": "saf-docker generate && docker compose --env-file .env.dev --env-file .env -f docker-compose.yaml up --watch"
  },
  "dependencies": {
    "@saflib/dev-tools": "*"
  }
}
